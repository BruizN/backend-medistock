[pytest]
asyncio_mode = auto
# Forzar que tanto fixtures como tests usen el mismo loop de sesión
asyncio_default_fixture_loop_scope = session
asyncio_default_test_loop_scope = session

log_cli = true